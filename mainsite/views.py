from django.shortcuts import render

# Create your views here.
# Create your views here.
def home(request):
    # return redirect('https://bobiipay.pay-monthly.com/')
    # 已由 nginx proxy_pass 處理，不會由 django 處理
    pass